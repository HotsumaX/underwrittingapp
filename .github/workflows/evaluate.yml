name: Evaluate Repository

on:
  push:
    branches:
      - master

jobs:
  evaluate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # Ensure latest v3 is used

    - name: Set up Node.js
      uses: actions/setup-node@v3  # Ensure latest v3 is used
      with:
        node-version: '20'

    - name: Set up Python
      uses: actions/setup-python@v4  # Use the latest v4 which supports Node.js 20
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run evaluation script
      run: python evaluate_repo.py

    - name: Append evaluation to README.md
      run: |
        cat progress_report.txt >> README.md
        git config --global user.name 'github-actions[bot]'
        git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
        git add README.md
        git commit -m 'Update README.md with progress report'
        git push